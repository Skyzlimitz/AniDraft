# @anidraft/anilist — Environment Variables
# Copy relevant values into your root .env.local.

# Optional AniList bearer token. Read queries work unauthenticated; a token only
# raises the rate-limit ceiling. Loaded by AniListClient via process.env.ANILIST_TOKEN.
ANILIST_TOKEN=
