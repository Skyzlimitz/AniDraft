/**
 * Database schema — stub file.
 *
 * Schema tables will be added by these issues:
 * - #39: users, anime, episodes, anilist_cache
 * - #40: drafts, picks, rosters
 * - #41: weekly_snapshots, activity_log, notification_events
 */

// Placeholder export so the module resolves
export {};
