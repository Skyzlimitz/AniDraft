name: Web Screenshot

on:
  pull_request:
    branches: [main, master]
    paths:
      - "apps/web/**"
      - "packages/**"
      - ".github/workflows/web-screenshot.yml"
  push:
    branches: [main, master]
    paths:
      - "apps/web/**"
      - "packages/**"
      - ".github/workflows/web-screenshot.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  screenshot:
    name: Build & Screenshot landing page
    runs-on: ubuntu-latest

    env:
      # `next build` runs with NODE_ENV=production, where webEnvSchema
      # (packages/shared/src/env.ts) requires these vars. See docs/env-vars.md.
      # CI-only throwaway values — no real services are reached.
      NEXT_PUBLIC_REALTIME_URL: ws://localhost:4000
      AUTH_SECRET: ci-only-screenshot-build-secret
      DATABASE_URL: file:./dev.db
      GOOGLE_CLIENT_ID: ci-only-google-id
      GOOGLE_CLIENT_SECRET: ci-only-google-secret
      DISCORD_CLIENT_ID: ci-only-discord-id
      DISCORD_CLIENT_SECRET: ci-only-discord-secret

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browser
        run: pnpm --filter web exec playwright install --with-deps chromium

      - name: Build web app
        run: pnpm --filter web build

      - name: Capture screenshot
        run: pnpm --filter web exec playwright test

      - name: Upload screenshot artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: landing-screenshot
          path: apps/web/screenshots/
          if-no-files-found: warn

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: apps/web/playwright-report/
          if-no-files-found: ignore
